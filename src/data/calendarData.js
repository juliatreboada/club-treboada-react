// src/data/calendarData.js
// Legacy entry point: event rows now live in Supabase `calendar_events`.
// Re-export taxonomy for any older imports.

export { categories, schools } from '../config/calendarTaxonomy.js';
